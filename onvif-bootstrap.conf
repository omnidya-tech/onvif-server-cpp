# Create the modem-info sentinel at boot so gps.service does not spin forever
# waiting for the LTE modem. Without this, the whole camera capture pipeline
# (dashcam.service → gps.service → /tmp/modem_info) hangs on units without
# active SIM, and RTSP live stream returns 503.
f /tmp/modem_info 0644 root root -
